export const MAX_ORGANIZATIONS = 3;

/** HttpOnly cookie so server routes keep the switched org after a reload. */
export const ACTIVE_ORG_COOKIE = "pz-active-org";

export const ACTIVE_ORG_STORAGE_KEY = "activeOrganizationId";
