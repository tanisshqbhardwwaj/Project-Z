Put release installers here so the pricing page can serve them:

- `project-z.apk` — Android
- `project-z-setup.exe` — Windows

These files are gitignored. After you copy them in, restart the Next.js server and the download buttons turn on.
